# Next Task

## Preserve M1 Setup-State Review as Display-Only Evidence

Keep the NAS100 Candle setup-state filter reconciled to the frozen 658-event
result: 31 filled, 32 formed including filled, 1 formed-but-unfilled, and 626
not formed. The complete-H4 M1 charts for zero-setup rows are visual evidence
only. Do not infer an unregistered setup from those charts, add execution
levels, simulate an entry, or change detector inclusion from presentation
work. Preserve `chartOnly: true`, `executionAuthorized: false`, the exact H4
interval boundary, and zero additional strategy trials.

## Intrabar D1 → H4 → M1 Correction — Terminal Reviewed Result

Preserve the corrected nested clock: D1 activation at a completed M1 close
without waiting for D1 close; same-direction H4 activation inside that D1
candle without waiting for H4 close; and M1 breaker/FVG search strictly after
activation through that H4 candle's own interval end. Do not restore the old
next-D1-close M1 window or use the close-confirmed 614/330 census as evidence
for this strategy.

The two frozen intrabar variants have positive gross expectancy but negative
expectancy under every normalized cost scenario. Candidate and champion remain
`NONE`; decision is `TECHNICAL_EDGE_NOT_FOUND`. The next permitted action is
read-only user review or bounded presentation correction. Do not tune the
hierarchy, pivots, breaker, FVG, entry, stop, target, expiry, or ambiguity rules
on this exposed history. A new economic claim still requires broker-confirmed
feed/execution metadata and a new prospectively frozen cycle.

## M1 Full-Wick Breaker + Displacement FVG — Terminal Negative Result

Preserve the completed two-trial result over the accepted 614 D1 sweeps and 330
H4-confirmed children. The 2R and 2.5R variants are both negative gross and
under normalized costs; candidate and champion are `NONE`, and the decision is
`TECHNICAL_EDGE_NOT_FOUND`.

Keep the prior nine M15/M5/M1 comparisons out of the active NAS100 Candle UI,
but preserve their artifacts, terminal negative decision, and cumulative trial
count. Preserve the new two-trial artifacts and charts as read-only evidence.
Do not tune the pivot, swing, displacement, FVG, breaker, entry, stop, target,
expiry, or path rules on this exposed history. Do not modify the frozen D1 or H4
registries.

## NAS100 D1-to-H4-to-LTF Comparison — Terminal Negative Result

Preserve the accepted 614 D1 sweeps, the 330/283/1 H4 state split, and all nine
frozen M15/M5/M1 entry configurations. All nine are negative gross and under
normalized medium costs. Candidate and champion are `NONE`; the decision is
`TECHNICAL_EDGE_NOT_FOUND`.

The next safe action is bounded user review of the charts and immutable result
table. Do not tune FVG, order-block, breaker, sweep, MSS, entry, stop, target,
expiry, or same-bar rules on this exposed history. Do not pool timeframes or
families after seeing outcomes. Any new technical cycle requires a materially
different, prospectively frozen hypothesis and genuinely new evidence, while
retaining these nine trials in cumulative selection accounting. Macro gating
remains blocked by uncertified point-in-time macro lineage.

## NAS100 Weekly Macro Bias — As-Of State Correction Complete

The user approved replacing the exact-observation-week reset with an explicit
latest-as-of state reconstruction for the display-only `Backtesting → NAS100`
weekly macro surface. For each of the 21 U.S. event definitions, every weekly
cutoff must use the most recent eligible observation whose stored reference
date is on or before that week's end. That observation remains active until a
newer eligible observation replaces it. Weeks before a series' first available
observation may remain `—`.

This is a state carry, not a fabricated release or interpolated Actual. Preserve
the source observation date, identify observations carried from an earlier
week, prohibit any observation after the weekly cutoff, and recalculate the
weekly NAS100 bias from the active 21-series state. Historical Forecast remains
`—` where unavailable. The current-vintage and non-point-in-time warning stays
mandatory; this correction does not authorize price, PnL, strategy selection,
or execution work.

The correction is complete and verified. The next permitted action is bounded
user review or presentation refinement that preserves the latest-as-of rule,
the original observation date, pre-inception dashes, June 2026 exclusion, and
the visible current-vintage/non-point-in-time warning. Preserve the weekly
detail modal's Policy, Growth, Labor, and Inflation cards as a presentation of
the existing historical `category_scores`; do not create a second formula or
allow the four active-series counts to stop reconciling to the event table. A
true historical macro backtest remains blocked on licensed as-published
consensus, release/receipt timestamps, and revision lineage.

## NAS100 Candle — Next Permitted Work

The read-only `Backtesting → NAS100 Candle` viewer is complete. The next safe
action is user visual review and bounded presentation refinement only. Preserve
the sibling navigation structure:

- `Backtesting → NAS100` at `/backtesting/nas100` for weekly macro bias;
- `Backtesting → NAS100 Candle` at `/backtesting/nas100-candle` for frozen
  technical evidence.

Do not turn NAS100 into a parent submenu and do not nest NAS100 Candle beneath
it. Do not rerun or tune the detector, rank setups by favorable outcome, add
macro gating, infer missing pre-2008 technical history, or add live/paper/order
actions. Any new technical research cycle requires a prospective governance
update and must preserve the frozen `TECHNICAL_EDGE_NOT_FOUND` result.

### Preserve hierarchy context labels

Keep filled/formed M1 setup charts bounded to and visibly labeled as their
complete `Owning H4 window`. Keep the H4 chart's active D1-window label. Keep
the D1 chart at a minimum of five candles by showing three earlier Daily
context bars plus the exact frozen C1/C2 swing pair; never append a later D1
bar to make the chart denser. These are presentation projections only. Do not
reinterpret a chart, create a setup from appearance, or change the frozen
breaker/FVG result, entry, stop, target, fill, outcome, or trial count.

## NAS100 Weekly Macro Bias — Next Permitted Work

The display-only `Backtesting → NAS100` weekly macro-bias surface is complete.
The next safe product action is user visual review and bounded presentation
adjustment without changing the formula.

A true historical macro backtest remains blocked. Before adding NAS100 prices,
technical bias, returns, PnL, or selection, onboard exact historical release
timestamps, licensed point-in-time consensus Forecast, Previous-as-published,
and revision lineage; then freeze a new prospective preregistration. Do not
treat the current-vintage observation-week reconstruction as information known
at the historical decision time. See
`NAS100_WEEKLY_MACRO_BIAS_BACKTESTING_TASK.md`.

## Preserve Backtesting Navigation Scope

Keep `NAS100` and `NAS100 Candle` as sibling leaves under Backtesting. Preserve
their authenticated display-only behavior and do not infer a backtest engine,
new strategy, data mutation, or execution workflow from either evidence page.

## Preserve U.S. Macro Official-Absence and Series-Inception Boundaries

The complete 21-series Trading Economics Public Chart refresh is finished for
all officially published Actual observations. Preserve the five official
shutdown-related absences and the four exact series-inception boundaries. Do
not interpolate them as Actuals or splice differently defined predecessor
series merely to create a dense January 2000 matrix.

The display-history task may proceed to the next user-approved application
step. Any point-in-time research step remains separately blocked until
historical forecasts, previous-as-published values, and exact release/receipt
timestamps are licensed and normalized.

Keep the Investing fallback frequency mapping separated: U.S. CPI m/m is event
page `69` and U.S. CPI y/y is event page `733`. Treat the parenthesized month on
Investing history rows as the reference period; never infer it from the release
date.

## Preserve Earnings Backdrop Terminology

Keep the blended Earnings Backdrop subtitle and its tooltip aligned on
`Growth + Labor` terminology. Do not revert the subtitle to `Activity + Labor`
or treat this copy clarification as a scoring change.

## Preserve NAS100 Composite Score Explanations

Keep the accessible question-mark help controls beside `Earnings Backdrop` and
`Fed Path Bias`. Positive values mean supportive for NAS100 within that channel,
negative values mean a headwind, and zero means mixed/neutral. Preserve the
warning that scores are net `+1`/`0`/`-1` event contributions rather than
percentages, probabilities, validated trade signals, or values that should be
added across the two channels. Do not change score arithmetic through tooltip
or presentation work.

## Keep Alignment Research Out of Primary Navigation

Do not restore `Alignment Research` to the Currency Pairs → Fundamental
Analysis sidebar unless the user explicitly requests it. Preserve the existing
historical research route and artifacts for auditability; hiding navigation is
not authorization to delete research evidence.

## Preserve NAS100 Scorecard Vocabulary Levels

Keep category-level reads and cross-category blended outlooks visually and
verbally distinct. Category boxes remain the only clickable score elements and
must retain the visible `View releases` affordance. Preserve `Activity` and
`Disinflation` category language, dual `Economy` / `Rates` Labor badges, and
the blended titles `Earnings Backdrop` and `Fed Path Bias` with their formulas.
Do not restore the repeated `Growth resilience` / `Rate-path support` labels in
the UI, create a flat overall score, or change the existing backend scoring
keys and arithmetic without a separately governed change.

## Preserve Persistent Sidebar and Native Link Semantics

Keep expanded sidebar module keys persisted across route transitions and page
remounts. Entering a route directly or in a new tab must open the route's
required parent hierarchy while retaining other user-opened sections. Keep
route-bearing leaf items as real internal links so normal click stays in the
current tab and standard browser Cmd/Ctrl-click, middle-click, context-menu,
and accessibility behaviors remain available. Do not convert Logout into a
route link or change authorization boundaries.

## Preserve Currency Fundamental Country Navigation and Hidden Lineage

Keep Currency Pairs → Fundamental Analysis → Countries available for United
States, United Kingdom, Eurozone, Australia, Canada, and Japan. Do not collapse
the navigation back to a U.S.-only direct link. Keep source/provider lineage in
the data contract and audit trail, but do not render a `Source` column in the
country overview or event-history tables unless the user explicitly requests
it. Preserve the existing economic fields, graph controls, impact behavior,
and separate macro-scorecard logic.

## Preserve Unified Stock Search Semantics

Keep the U.S. Stock Markets text search unified across ticker, company,
historical alias, sector, theme/category, and nuclear subcategory. A category
name or partial category name must directly show the matching category card in
theme view and its member stocks in table view. Preserve intersection with the
explicit sector and theme filters. Do not re-expand a matching category query
into unrelated overlapping category cards.

## Preserve Provider-State Session Display

Keep U.S. stock extended-price display keyed to Moomoo's provider-reported
`market_us` state rather than a fixed Malaysia clock. Regular trading must show
no premarket, post-market, or overnight secondary row. Premarket must show only
premarket; post-market must show only post-market; overnight/closed-after-hours
must prefer overnight and may fall back to the latest post-market value when an
overnight quote is unavailable. Unknown states must fail closed rather than
show stale premarket data.

Preserve the 15-second REST fallback throughout overnight because Moomoo's
quote subscription explicitly excludes overnight streaming. Holiday and early
close handling remains provider-state-owned; do not replace it with a permanent
UTC or Kuala Lumpur offset. The prominent top price remains unchanged.
Keep every visible extended-session strip single-line and equal-height; longer
prices must not expand one card's secondary border.

## Preserve Stock Stream Fallback and Coverage Disclosure

Keep the Moomoo stock WebSocket additive to the existing REST snapshot. Never
disable 15-second polling unless the worker reports healthy, complete coverage.
With the current 100-of-239 Moomoo subscription entitlement, retain hybrid mode
and the `Live + 15s fallback` disclosure. A disconnect, malformed payload,
upstream health failure, or stale heartbeat must return to polling without user
action. Keep the WebSocket loopback-only and origin-allowlisted.

Full real-time coverage for all 239 symbols requires a Moomoo entitlement of at
least 239 simultaneous stock quote subscriptions or a separately authorized
streaming provider. Do not hide the quota by rotating subscriptions and calling
the resulting snapshots fully real-time. Production `wss://` proxying and
service supervision remain separate deployment work requiring an explicit
target environment and authorization.

## Preserve Stock Theme Category Ranking Semantics

Keep the U.S. Stock Markets category performance control display-only. Default,
best-first, and worst-first ordering must use the selected 1D/1W/1M percentage
change and the currently visible stocks after search, sector, and theme filters.
Exclude missing history rather than converting it to zero, and keep categories
with no usable observations after categories with valid averages. Do not treat
this live category ordering as a strategy score, prediction, or trade signal.

## Preserve NFP Alias and Release-Bundle Consistency

Treat provider spellings `Nonfarm Payrolls` and `Non-Farm Employment Change`
as the same canonical NFP event. The country table, currency scorecard, and
NAS100 scorecard must select one coherent Employment Situation release date;
do not combine an older NFP release with newer Unemployment Rate or Average
Hourly Earnings observations. Preserve Forex Factory ownership of event,
Forecast, and Previous and the independent Actual-provider lineage.

## Preserve Four Separate US CPI Frequency Contracts

Keep Core CPI m/m, Core CPI y/y, CPI m/m, and CPI y/y separate in country
cards, history graphs, provider normalization, and macro-category evidence.
Never allow a newer m/m observation to occupy a y/y slot or vice versa. Future
calendar releases should continue to use Forex Factory for event identity,
Forecast, and Previous, with the configured actual-provider priority. Public
Trading Economics chart history remains display-only and must not be treated as
point-in-time consensus or release-clock evidence.

## Live Trading Economics Actuals — Credential Required

The FF event/Forecast/Previous to provider-Actual linkage is implemented and
scheduled. To make Trading Economics the live actual source, configure a valid
`TRADING_ECONOMICS_API_KEY` with calendar access and rerun the bounded provider
contract tests. Until then, Investing.com is the live fallback and the public TE
chart history remains display-only. Do not infer or copy a public chart value
onto an FF release row when exact release identity cannot be proven.

## Economic Calendar Eligibility — Preserve Numeric-Only Boundary

Keep `testifies`, `speaks`, `report`, and `statement` event titles excluded at
ingestion and API display. Do not reintroduce them as pending or neutral numeric
fundamental observations. If qualitative central-bank communication is added in
future, it requires a separate timestamped news/communication contract rather
than empty Actual/Forecast/Previous calendar fields.

## NAS100 Macro Scorecard — Preserve Interpretive Boundary

The separate NAS100 scorecard and U.S. event view are complete. Preserve the
two-channel `Growth Resilience` / `Rate-Path Support` model, uncapped category
evidence, explicit USD-to-NAS100 translation, and absence of a flat overall
score. Do not convert this display into a trade trigger or use current-vintage
Trading Economics chart history for historical scoring.

Any empirical NAS100 weighting, predictive validation, or execution use
requires a new prospective preregistration, point-in-time release/forecast/
revision evidence, and an explicit broker instrument/feed identity. None is
authorized by this UI task.

## Alignment Review — Complete, No Strategy Continuation

The separately governed read-only viewer at `/research/alignment-review` is
complete. Preserve its frozen 454-setup population, medium-cost outcome
projection, J0 macro links, immutable input hashes, and `UNKNOWN` macro status.
Do not replace the point-in-time macro result with current-vintage Trading
Economics chart history and do not add a live entry/order action.

There is no permitted tuning or strategy continuation from this viewer. A
future quantitative continuation still requires genuinely point-in-time macro
release/forecast/revision evidence and a new prospective preregistration.

## Active US Macro History Backfill — 2000 Through 2026

Task `TE-US-MACRO-INGESTION-2000-2026-002` prospectively expands the separate
read-only application-data onboarding range to 2000-01-01 through 2026-07-15.
Only United States observations for the 21 user-approved indicators may be
collected. Existing rows, including the current 2020-2026 history, must remain
unchanged; collection inserts only missing provider versions. Non-US scorecard
rows remain visible but are not in collection scope. The five removed liquidity
indicators are excluded.

Public Trading Economics history pages are the first candidate under a strict
one-request-at-a-time, cached, no-bypass policy. Login, subscription, CAPTCHA,
Cloudflare, robots, rate-limit, or other access controls must not be bypassed.
Post-2026-06-28 observations are application-display-only and are ineligible for
strategy research.

Status: `COMPLETE_DISPLAY_ONLY_WITH_POINT_IN_TIME_LIMITATIONS`. The public
history backfill is complete and graph-visible. Do not treat it as as-published
release evidence or use it for macro surprise scoring. The separately governed
read-only macro/technical alignment evidence viewer described above is also
complete; preserve it without recomputation, tuning, or a live trade action.

## Superseded Authenticated-API Onboarding Path

The user has prospectively authorized a separate, non-strategy data-onboarding
task under `TRADING_ECONOMICS_US_MACRO_INGESTION_PREREGISTRATION.yaml`.
Its first bounded range is United States releases from 2020-01-01 through
2026-06-30. This task may extend the read-only economic-data and macro-scorecard
surfaces and may insert provider-authenticated, immutable Trading Economics
observations into the application database. It must not reopen the terminal
macro strategy, alter frozen scores or joins, inspect PnL for selection, or use
post-cutoff releases.

Collection is permitted only through an authorized Trading Economics API
credential or another explicitly permitted Trading Economics export. Missing
authentication, subscription denial, CAPTCHA, 403, or an automated-access block
must fail closed; no bypass is permitted.

## Macro Regime Program — Terminal State, No Next Role

Program: `SMART-MARKETSCOPE-MACRO-REGIME-NAS100-001`

Full requested-program status is
`PROGRAM_COMPLETE_NO_ACCEPTABLE_STRATEGY_FOUND`. Role 11 independently accepted
the negative quantitative result and the bounded reporting/security remediation.
Candidate and champion remain `NONE`.

The exact `/research/macro-regime` route is active in the user-owned unstaged
`src/App.js`; its two declared hunks reconstruct and roll back to the exact
authorized baseline. Server-verified authentication, explicit read-only
authorization, query/fragment/extra-path negative IDOR, no-write/no-secret,
content, accessibility, and all 11 A-K chart hash controls pass.

There is no next sequential role. Preserve the immutable artifacts, terminal
negative decision, active route ownership boundary, and rollback patch. Do not
run more macro thresholds, weights, category rules, technical joins, PnL search,
or selection on the exposed history. A future research attempt requires a new
prospectively governed program and genuinely new evidence; it must not reopen or
tune this terminal program.

No broker connection, paper trading, order placement, deployment, live control,
or final-holdout access is authorized.

## Macro Liquidity Reversal Gate

The authorized technical economic continuation is terminal with
`TECHNICAL_EDGE_NOT_FOUND`. Do not run further parameter search or ML. The next
permitted MLR action is external evidence onboarding and independent
certification only: exact point-in-time macro release/receipt lineage and
Pepperstone NAS100 instrument/feed metadata.

Do not continue the full MLR strategy. First certify immutable macro source-run
and observation records with exact historical release/receipt and as-published
values, then obtain Pepperstone NAS100 source-timezone and symbol/spread metadata.
The exact requirements are in `MLR_NEXT_TASK.md`. Do not reconstruct unavailable
timestamps or access post-2026-06-28 data for tuning.

Status: `BLOCKED_EXTERNAL_POINT_IN_TIME_EVIDENCE`

## Task ID

`PROGRAM2-PIT-RELEASE-LINEAGE-003`

## Current Block

The point-in-time contract and a real bounded ALFRED bundle pass validation. The
bundle has 25 source runs and 1,730 initial/revision rows, but eligible count is
zero. ALFRED supplies date-level vintage lineage, not authoritative exact source
wall-clock, historical first-receipt, consensus forecast, or
previous-as-published lineage. Historical signed sentiment still lacks
contemporaneous receipt and model/prompt/schema hashes. Cycle 3 cannot be
preregistered from excluded rows or retrospectively rescored articles.

The 2026-07-15 CPI incident adds a mandatory canonical-identity gate: country,
indicator family, seasonal basis, frequency (`m/m`, `y/y`, `q/q`), unit,
reference period, release timestamp, and provider series ID must agree before a
row may enter a Program 2 feature bundle. Display labels are not identity. The
current Trading Economics public history fails the release-clock/vintage part of
this gate even where its current-vintage value matches an official comparator.

## Next Permitted Work

1. Join the frozen ALFRED series/reference periods to authoritative exact source
   release clocks. Preserve source timezone, DST-aware UTC/Malaysia conversion,
   schedule revisions, source URLs, and payload/code/config hashes.
2. Onboard a licensed point-in-time forecast source for surprise features,
   retaining forecast and previous exactly as published. If unavailable, keep
   the surprise family blocked rather than substituting later values.
3. Start or verify passive protected news collection that freezes `published_at`,
   first receipt, revisions/retractions, content hash, and exact as-received
   sentiment model/prompt/schema hashes. Do not inspect it for outcomes.
4. Pass a bundle with nonzero eligible rows through the existing independent
   validator and demonstrate coverage sufficient for both required families.
5. Add automated cross-frequency collision checks (including headline CPI m/m
   versus y/y and Core CPI m/m versus y/y) to the independent bundle validator.
6. After enough evidence exists, prospectively generate at least five candidate
   proposals, freeze approximately 70/30 exploit/explore allocation, and create
   Cycle 3 preregistrations before empirical code or outcome access.

## Invariants

- Do not fabricate historical first-received or exact release times.
- Do not use current-vintage revisable macro data as historical first prints.
- Do not rescore historical news and call it as-received.
- Do not open protected-forward data for feature design or tuning.
- Do not append an experiment registry event until a complete preregistration is
  hash-locked.
- Do not build/deploy paper infrastructure or connect a broker without a frozen
  provisional champion and separate authorization.

## Unblock Evidence

- Real provider bundle validator status `PASS` with nonzero eligible rows.
- Eligible row counts and coverage sufficient for both required families.
- Frozen source/feature manifests and leakage audit with no critical veto.
- A mature protected forward evaluation window reserved for one frozen promotion
  decision.

Until those facts exist, `BLOCKED_BEFORE_CYCLE_3_PREREGISTRATION` is the only
defensible state.

## D1 Sweep Review Next Action - 2026-07-15

Review the corrected 614-event D1 census in `Backtesting / NAS100 Candle`.
Do not run H4 or lower-timeframe entry logic until the D1 definition is accepted.
After acceptance, preregister one H4 confirmation window and expiry using the
same immediate-prior-candle wick-and-reclaim rule. This bounded technical review
does not change the separate Program 2 macro-data block above.

# MLR Mechanical Specification

Program: `QRP-MACRO-LIQUIDITY-REVERSAL-001`  
Authoritative skill: `.agents/skills/multi-timeframe-macro-liquidity-reversal/`  
Frozen skill-set SHA-256: `83d2e96e8435398571814b340fb5c181f42361ec790c239d7337b3668c1eb050`

The skill's `MECHANICAL_SPEC.md` is the source of truth. The implementation uses
completed bars, SMA-seeded EMA20/EMA50, SMA-seeded Wilder ATR14, close-only
two-candle sweeps at ratio 0.50, the third post-D1-close H4 extension, strict
three-bar FVGs, displacement-first OB selection with the latest opposite candle
inside K=3, first-retouch breakers, strict positive overlap, midpoint entry,
block-based stop, exact cost-inclusive 2R, adverse-first unresolved same-bar
ordering, one trade per D1 setup, and native D1 candle-3 expiry.

M15, M5, and M1 standalone architectures are separate. The hierarchy uses the
first M15 zone, first later same-family overlapping M5 zone, and first later
same-family overlapping M1 zone. C1 and C2 cannot be mixed in one hierarchy.

No generalized ICT/SMC interpretation is permitted. Macro direction is consumed
from a certified contract and is never generated by these detectors.

